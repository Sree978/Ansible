install/uninstall/update
files create & users
users create
webserver up
webserver down
restart
cpu info
mem info
copy

single line command  its ansible modules  we wont use much
will use for quick actions
ansible group-name -m <module> -a "args"

ansible dev -m yum -a "pkg=tree state=present"

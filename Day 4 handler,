Handler

Vim playbook.yml

---
- hosts: dev
  connection: ssh

  tasks:
    - name: install webserver
      yum:
        name: httpd
        state: present
      notify:
        - start webserver

  handlers:
    - name: start webserver
      service:
        name: httpd
        state: started
...

ansible-playbook playbook.yml

once installtion only handler will start

------->  Roles   <-------------

[root@master ~]# tree
.
├── flm
    ├── maim.yml
│   ├── adidas
│   │   └── task3
│   │       └── task3.yml
│   ├── hrx
│   │   └── task
│   │       └── task1.yml
│  
│   └── puma
│       └── task2
│           └── task2.yml
└── playbook.yml

go to flm flder and run the main.yml
you will see all the task completed o/p you can see over browser

--------------> Ansible debug module <----------------
 to check applications installed or not in server over playbook


 vim debug.yml

---
- hosts: dev
  connection: ssh

  tasks:
    - debug:
        msg:  "we are learnig pipiline"

ansible-playbook debug.yml


---
- hosts: dev
  connection: ssh

  tasks:
    - name: check the git version
      command: git -v
      register: abc

    - debug:
        msg: "The SCM tool is {{ abc.stdout }}"








